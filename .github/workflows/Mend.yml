# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  workflow_call:
    inputs:
      config_file:
        description: 'Path of Mend Config file'
        default: 'wss-unified-agent.config'
        required: true
        type: string
      product_Name:
        description: 'Application Name'
        required: true
        type: string
      project_Name:
        description: 'Module Name'
        required: true
        type: string
    secrets:
      apikey:
        description: 'API key from Welcome email'
        required: true
      userkey: 
        description: 'User Key from your Mend profile/ service account'
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      # Download unified agent jar file
      - name: Download the unified agent scanner
        run: curl -LJO https://github.com/whitesource/unified-agent-distribution/releases/latest/download/wss-unified-agent.jar
      # Runs Mend scan
      - name: Mend Scan
        run: java -jar wss-unified-agent.jar -c ${{ secrets.config_file }} -apikey ${{ secrets.apikey }} -userkey ${{ secrets.userkey }}
